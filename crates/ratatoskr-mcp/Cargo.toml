[package]
name = "ratatoskr-mcp"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
ratatoskr-core.workspace = true
thiserror.workspace = true
tracing.workspace = true
# rmcp carries client + transport-child-process features (see workspace Cargo.toml); the version
# is pinned to 2.x to match rig-agent, so the ServerSink handed to `.rmcp_tools()` type-checks.
rmcp.workspace = true
tokio = { workspace = true, features = ["process"] }

[lints]
workspace = true
